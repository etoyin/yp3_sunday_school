<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link href="/styles/output.css" rel="stylesheet">
</head>
<body class="h-screen bg-gray-100">
    <%- include('partials/_navbar') %>
    <div class="container px-4 py-8 mx-auto">
        <div class="max-w-md p-6 mx-auto mt-10 bg-white rounded-lg shadow-lg">
            <div class="flex items-center justify-center w-12 h-12 mx-auto mb-4 bg-red-100 rounded-full">
                <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
            </div>
            <h1 class="mb-4 text-2xl font-bold text-center text-red-600">Error</h1>
            <p class="mb-6 text-center text-gray-700"><%= message %></p>
            <% if (error && error.status) { %>
                <p class="mb-2 text-sm text-center text-gray-500">Status: <%= error.status %></p>
            <% } %>
            <div class="text-center">
                <a href="/" class="inline-block px-4 py-2 font-bold text-white bg-blue-500 rounded hover:bg-blue-700 focus:outline-none focus:shadow-outline">
                    Return to Home
                </a>
            </div>
            <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
                <div class="p-4 mt-6 overflow-auto text-xs bg-gray-100 rounded">
                    <pre><%= error.stack %></pre>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
